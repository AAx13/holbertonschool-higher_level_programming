
def odd(i):    # here im defining a function that will be called from another file.
    if i % 2 == 0: # if the number passed as a parameter for a function called from another file is divisible by 2 then return 0.
        return 0
    else:
        return 1
